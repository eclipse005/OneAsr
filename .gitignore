/target
/runs
/output
# Bundled ffmpeg is large; place bin/ffmpeg.exe locally for dev/run
/bin/*.exe
# Downloaded ASR/aligner weights (multi-GB; never commit)
/models
# CUDA DLLs installed next to the exe / into dll/
/dll
# User preferences (local machine paths)
/settings.json
# Crash/error log written next to the exe (rotated to .old)
/oneasr-error.log
/oneasr-error.log.old
# Release packaging output
/dist
/release
# Local-only: packing scripts + forum draft (not for public repo)
/scripts/
/forum-post.md
# Icon gen intermediates
assets/icons/_t*.png
**/*.rs.bk
.DS_Store
*.pdb
# Local evaluation audio (large; keep manifest/README in git if desired)
/testdata/clips/
/testdata/refs/
/testdata/gold/
/testdata/hyps/
/testdata/baselines/
/testdata/_cache/
/testdata/_compare*/
