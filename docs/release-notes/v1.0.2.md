## OneAsr 1.0.2 — 本地离线音视频转字幕

这一版把 ASR、强制对齐、人声分离三个引擎的底层库更新到最新优化版本，并重做了设置面板的保存机制。识别与对齐的结果和之前完全一致，处理速度全面提升，对齐模型的加载时间明显缩短。

### 新增

- **识别更快**：ASR 引擎更新到最新优化版本，prefill 与 decode 全链路均有可测量的提速，识别结果与之前逐位一致。
- **对齐更快**：强制对齐引擎更新到最新优化版本，模型加载时间大幅缩短；长音频的重采样、编码与首次对齐（含日语分词预热）也更快。
- **人声分离更快**：人声分离引擎更新到最新优化版本，分块推理流水线化并融合多个算子，同一段音频的分离耗时更短。
- **设置面板新增「重置」按钮**：左下角一键把全部设置恢复初始值；和所有改动一样，重置也是临时的，点「保存设置」后才生效。

### 修复

- **设置改动统一为保存后才生效**：以前选完模型 / 输出目录就立即写盘，和其他设置项不一样；现在所有改动都是临时的，点「保存设置」才真正生效。
- **关闭设置面板等同于取消**：未保存的改动全部回滚到上次保存的状态，不再自动保存。
- **模型目录选择不再有多余的自动解析**：选什么目录就原样用作当前型号（0.6B / 1.7B）的目录，不再自动识别同目录下的其他型号文件夹；没选过的型号才用安装目录下的 models 路径，下载模型也会下到当前选定的目录。

### macOS 安装

包未签名（没有 Apple 开发者账号），首次安装需要解除一次系统的隔离标记，装好之后不再提示。磁盘映像里附了「安装说明.txt」和「安装 OneAsr.command」：

1. 先右键「安装 OneAsr.command」→ 选「打开」→ 弹框里再点一次「打开」；
2. 如果 macOS 15 起把这条路拦了（弹框里只剩「完成」），改用终端：

```bash
bash "/Volumes/OneAsr 1.0.2/安装 OneAsr.command"
```

（终端里敲 `bash` 加一个空格，然后把脚本图标拖进终端窗口，路径会自动补上。）

### 下载

| 平台 | 文件 |
|------|------|
| macOS（M 芯片） | `OneAsr_1.0.2_macos.dmg` |
| Windows | `OneAsr_1.0.2_windows_setup.exe`（安装包）· `OneAsr_1.0.2_windows_portable.zip`（便携包） |
| Linux x64 | `OneAsr_1.0.2_linux_x64.deb` · `OneAsr_1.0.2_linux_x64.tar.gz` |

首次运行会下载语音识别模型（几百 MB 起），下完即可离线使用，识别全程在本机进行。

**完整对比**：https://github.com/eclipse005/OneAsr/compare/v1.0.1...v1.0.2
